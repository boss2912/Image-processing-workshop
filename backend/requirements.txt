# ล็อกเวอร์ชันไว้ทุกตัว เพราะทั้ง 3 คนต้องได้ภาพผลลัพธ์เหมือนกันเป๊ะ
# ถ้าคนหนึ่งได้ OpenCV คนละรุ่น ผลของ Canny/Harris อาจต่างกันเล็กน้อยโดยไม่รู้ตัว
# ชุดนี้ทดสอบผ่านแล้วบน Python 3.12 (Windows) — รัน backend ได้จริงทั้ง canny และ harris

Flask==3.0.3
Werkzeug==3.1.8
flask-cors==6.0.5
opencv-python==4.10.0.84
numpy==2.3.5

# หมายเหตุ
# - Werkzeug : Flask เรียกใช้ภายใน ถ้าไม่ล็อกไว้ pip อาจดึงรุ่นใหม่ที่ Flask 3.0.3 ยังไม่รองรับ
# - opencv-python : ห้ามติดตั้ง opencv-contrib-python ควบคู่กัน ทั้งสองแพ็กเกจเขียนทับ
#   โฟลเดอร์ cv2/ เดียวกัน ตัวที่ติดตั้งทีหลังจะทับตัวแรก แล้ว pip list จะรายงานคนละเวอร์ชัน
#   กับที่ cv2.__version__ บอกจริง (ดูหัวข้อ "แก้ปัญหาที่เจอบ่อย" ใน README)
# - numpy : numpy 2.x กับ 1.x ใช้ ABI คนละแบบ ถ้าไม่ล็อกอาจเจอ error ตอน import cv2
#
# ถ้าอยากล็อกละเอียดกว่านี้ (รวมแพ็กเกจลูกทุกตัว) ให้ใช้
#   pip freeze > requirements.lock.txt
